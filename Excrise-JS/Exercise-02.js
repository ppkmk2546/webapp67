const random_Number_In_Range = (min, max) => 
    Math.random() * (max - min) + min;


console.log(random_Number_In_Range(2,10))

/*
? อธิบายโค้ด
    ? 1.const random_Number_In_Range = (min, max) =>:
        *นี่เป็นการประกาศฟังก์ชันชื่อ random_Number_In_Range ซึ่งรับค่าพารามิเตอร์สองตัวคือ min (ค่าต่ำสุด) และ max (ค่าสูงสุด)
        *ฟังก์ชันนี้เขียนในรูปแบบ Arrow Function (=>)
        
    ? 2.Math.random():
        *ฟังก์ชัน Math.random() จะคืนค่าตัวเลขทศนิยมสุ่มที่อยู่ในช่วงตั้งแต่ 0 ถึง 1 (แต่ไม่รวม 1)
        *ตัวเลขที่คืนค่าออกมา เช่น 0.123456 หรือ 0.987654 ซึ่งสามารถใช้สร้างค่าตัวเลขสุ่มได้
        *(max - min):

    ? 3.(max - min):
        *การลบ min จาก max เพื่อหาช่วงของค่าที่ต้องการสุ่ม เช่น ถ้า min = 2 และ max = 10 ผลลัพธ์คือ 8 ซึ่งหมายถึงช่วงของค่าตัวเลขสุ่มที่ต้องการคือระหว่าง 2 ถึง 10
        *Math.random() * (max - min):

    ? 4. Math.random() * (max - min):
        *ค่านี้จะทำให้ได้ตัวเลขทศนิยมสุ่มในช่วงตั้งแต่ 0 ถึง (max - min) ซึ่งเป็นการสุ่มค่าภายในช่วงที่ต้องการ เช่น ถ้าช่วงคือ 8 ตัวเลขที่ได้จากการสุ่มจะอยู่ในช่วงตั้งแต่ 0 ถึง 8
    ? 5. + min:
        *การบวก min เข้าไปเพื่อทำให้ตัวเลขสุ่มที่ได้เริ่มต้นที่ค่าต่ำสุด (min) ตัวอย่างเช่น ถ้าค่า min = 2 ผลลัพธ์ที่ได้จะอยู่ในช่วงตั้งแต่ min ไปจนถึง max (เช่น 2 ถึง 10)
 */
  